╔══════════════════════════════════════════════════════════════╗
║          🎯 SIMPLE FIX - FOLLOW THESE 3 THINGS              ║
╚══════════════════════════════════════════════════════════════╝

Your backend IS running, but MongoDB is BLOCKED!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

THING 1: MONGODB ATLAS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go here: https://cloud.mongodb.com/

2. You'll see something like this:

   ┌─────────────────────────────────────┐
   │ Organizations ▾                     │
   │ Projects        ▾                   │
   │                                     │
   │ ← Overview                          │
   │ ← Database                          │
   │ ← SECURITY                          │
   │    ← Network Access  👈 CLICK THIS  │
   │    ← Database Access                │
   └─────────────────────────────────────┘

3. Click "Network Access"

4. You'll see this page:

   ┌─────────────────────────────────────────────┐
   │  Network Access                             │
   │  ┌──────────────────────────────────────┐  │
   │  │ [+  ADD IP ADDRESS ]  👈 CLICK THIS  │  │
   │  └──────────────────────────────────────┘  │
   │                                             │
   │  IP Access List:                            │
   │  (might be empty or have some IPs)          │
   └─────────────────────────────────────────────┘

5. A popup appears:

   ┌─────────────────────────────────────────┐
   │  Add IP Access List Entry               │
   ├─────────────────────────────────────────┤
   │  ○ Add Current IP Address               │
   │  ● Allow Access from Anywhere  👈 CLICK │
   │                                          │
   │  Access List Entry:                      │
   │  0.0.0.0/0                              │
   │                                          │
   │  Comment (optional):                     │
   │  Allow from anywhere                     │
   │                                          │
   │  [  Cancel  ]  [  Confirm  ] 👈 CLICK   │
   └─────────────────────────────────────────┘

6. After clicking Confirm, you'll see:

   ┌─────────────────────────────────────────────┐
   │  IP Access List:                            │
   │  ┌────────────────────────────────────────┐ │
   │  │ 0.0.0.0/0                              │ │
   │  │ Status: PENDING → then ACTIVE          │ │
   │  │ Comment: Allow from anywhere           │ │
   │  └────────────────────────────────────────┘ │
   └─────────────────────────────────────────────┘

7. WAIT 2 MINUTES for status to change to "ACTIVE"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

THING 2: RENDER ENVIRONMENT VARIABLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go here: https://dashboard.render.com/

2. Click on "organic-ecommerce-yr0g"

3. You'll see left sidebar:

   ┌─────────────────────┐
   │ Events              │
   │ Logs                │
   │ Shell               │
   │ Metrics             │
   │ Environment  👈 CLICK│
   │ Settings            │
   └─────────────────────┘

4. Click "Environment"

5. You'll see environment variables page.
   Add these EXACT variables (copy-paste!):

KEY: NODE_ENV
VALUE: production
[Add Variable]

KEY: MONGODB_URI
VALUE: mongodb+srv://mehak24_db_user:mehak15@organicwebsite.jfcdjaq.mongodb.net/organicshop?retryWrites=true&w=majority&appName=organicwebsite
[Add Variable]
⚠️  Copy as ONE LINE, no breaks!

KEY: JWT_SECRET
VALUE: e50568bd74cf3f7ca03b8ece82a52009b81800a0c3b8e1d32defd0a0b9a29757
[Add Variable]

KEY: JWT_EXPIRE
VALUE: 7d
[Add Variable]

KEY: JWT_COOKIE_EXPIRE
VALUE: 7
[Add Variable]

KEY: FRONTEND_URL
VALUE: https://your-frontend.onrender.com
[Add Variable]
⚠️  Replace with your actual frontend URL!

6. Click "Save Changes" at bottom

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

THING 3: REDEPLOY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Still on Render (after saving environment variables)

2. Scroll to top, you'll see:

   ┌─────────────────────────────────────────┐
   │ organic-ecommerce-yr0g                  │
   │ ● Live                                  │
   │ [Manual Deploy ▾]  👈 CLICK THIS       │
   └─────────────────────────────────────────┘

3. Click "Manual Deploy" dropdown

4. Click "Deploy latest commit"

5. You'll see deployment in progress:
   
   Deploying... (takes 2-3 minutes)
   ├─ Installing dependencies
   ├─ Building
   └─ Starting server

6. When done, you'll see:
   ● Live (green dot)

7. Click "Logs" tab, look for:

   ✅ MongoDB Connected: ac-kvwncyc...
   ✅ Database: organicshop
   🚀 Server is running on port 5000

   If you see these ✅ messages = SUCCESS!

   If you see ❌ errors about MongoDB:
   → Go back to Thing 1 and make sure you did it correctly

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🧪 TEST IT:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run this in your terminal:

cd /home/sama/Desktop/organic\ website/organic-shop
./test-deployment.sh

Should show:
✅ Backend is running!
✅ Database is connected!

Then test in browser:
1. Clear cache (Ctrl+Shift+Delete)
2. Try to register with NEW email
3. Should work now! 🎉

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IF STILL NOT WORKING:

Did you:
[ ] Complete THING 1 (MongoDB IP whitelist)
[ ] Wait 2 minutes after Thing 1
[ ] Complete THING 2 (All 6 environment variables)
[ ] Complete THING 3 (Redeploy)
[ ] Check logs show "MongoDB Connected"

If YES to all and still fails:
- Take screenshot of Render → Environment page
- Take screenshot of Render → Logs (last 50 lines)
- Take screenshot of MongoDB → Network Access
- Share these for debugging

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
